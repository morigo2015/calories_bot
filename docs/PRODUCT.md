# PRD: Telegram-бот обліку калорій

**Статус:** актуальна реалізована версія станом на 9 серпня 2026 року.

## 1. Призначення

Бот дозволяє невеликій закритій групі користувачів швидко записувати з’їдену
їжу. Користувач надсилає опис або одне фото, бот визначає склад і відсутні
значення, рахує калорії, зберігає прийом та показує денний підсумок.

Бот працює лише у приватних Telegram-чатах. Групи, канали, редагування
повідомлень, фотоальбоми й документи не підтримуються.

## 2. Доступ і дані користувача

- Доступ надається одноразовим invite-посиланням адміністратора.
- `/start <invite_token>` активує користувача та створює окремий Google
  Spreadsheet. Повторний `/start` не створює нову таблицю.
- Користувач може бути `invited`, `active` або `blocked`.
- Заблокований користувач не читає й не створює записи, але його дані
  зберігаються.
- Історія, збережені страви та фото різних користувачів ізольовані.

Перед завантаженням фото або зверненням до моделі бот перевіряє доступ.

## 3. Запис їжі

Бот приймає короткий текст або одне фото з підписом чи без нього. Явно задані
вага та калорійність мають пріоритет над оцінкою моделі. Оцінені значення
позначаються `≈`.

Підтримуються, зокрема:

```text
сир 150 г
два яйця і бутерброд
сир 150 г 120#
120 г сиру, 300 ккал на 100 грам
```

`#120`, `120#` і природний запис `120 ккал на 100 грам` означають
`120 ккал/100 г`. Числа словами українською або російською нормалізуються.
Десяткові значення не підтримуються.

Один успішний текст, одне фото або одне натискання повторного додавання
створює один прийом. Повторна обробка тієї самої Telegram-події не створює
дубль. Нерозпізнана їжа або некоректний формат нічого не записують.

## 4. Результат і видалення

Бот показує калорійність кожного компонента, суму складеної страви та денний
підсумок. У звичайної нової страви є кнопки:

```text
[ ⭐ Запам’ятати ] [ Видалити ]
```

`Видалити` прибирає рядок прийому й пов’язане локальне фото, перераховує
відповідну облікову добу та замінює відповідь на новий підсумок. Початкове
повідомлення користувача лишається. Повторне видалення безпечне.

## 5. Збережені страви

`⭐ Запам’ятати` зберігає весь показаний прийом як одну страву з усіма
компонентами. Введення назви не потрібне. Після успіху кнопка збереження
зникає, а `Видалити` залишається.

Додатково `/save` зберігає останній прийом, а `/save <назва>` дозволяє явно
задати назву. Повторне збереження того самого прийому не створює дубль.
Однакові автоматичні назви отримують суфікси `(2)`, `(3)` тощо; явно задана
зайнята назва відхиляється.

`/meals` показує всі збережені страви одним прокручуваним переліком без
пагінації, найновіші зверху:

```text
[ Вівсянка з бананом · 320 г ] [ ⚖️ ]
[ Курка, гречка та салат · 440 г ] [ ⚖️ ]

[ Нещодавні ] [ Керувати стравами ]
```

- Натискання назви одразу додає стандартну порцію без нового аналізу.
- `⚖️` дозволяє один раз ввести іншу загальну вагу; усі компоненти
  масштабуються пропорційно.
- Разова вага не змінює стандартну.
- Для збереженої страви можна змінити назву, стандартну вагу або видалити її з
  підтвердженням.
- Зміни шаблону не змінюють історичні прийоми.

## 6. Нещодавні

Кнопка `Нещодавні` в `/meals` показує до восьми останніх різних невидалених
страв, найновіші зверху. Вибраний прийом можна:

- повторити в попередній вазі;
- додати з іншою загальною вагою;
- запам’ятати, якщо він ще не походить зі збереженої страви;
- повернутися до переліку.

Повторення використовує готовий попередній розрахунок і не звертається до
моделі. Стара кнопка видаленого джерела або видаленого шаблону нічого не
додає.

## 7. Підсумки й ціль

- `/day` показує суму поточної облікової доби та прийоми, згруповані за
  назвою.
- `/week` показує поточну облікову добу і шість попередніх, середнє, мінімум,
  максимум та дні в межах цілі.
- `/goal [кк]` встановлює денну ціль. `/goal` без аргументу відкриває короткий
  діалог зміни або вимкнення.

Кожен користувач має власний `day_start`, типовий — `01:00`, у часовому поясі
застосунку. Записи до цієї межі належать попередній обліковій даті. Зміна межі
не перераховує історію.

## 8. Команди

У меню користувача:

- `/meals` — збережені й нещодавні страви;
- `/day` — прийоми поточної доби;
- `/week` — підсумок за сім днів;
- `/goal` — денна ціль;
- `/help` — основна довідка;
- `/tips` — додатковий синтаксис.

`/save [назва]` підтримується, але не займає місця в меню.

## 9. Адміністрування

Адміністратору додатково доступні `/invite`, `/users`, `/block`, `/unblock` і
`/delete`.

`/invite <ім’я>` одразу створює посилання. Якщо `/invite` вибрано з меню без
аргументу, бот просить ім’я з кнопкою `Скасувати`, а наступний текст створює
invite й не обробляється як їжа.

`/delete <telegram_user_id>` після підтвердження повністю видаляє персональний
Spreadsheet, фото й запис реєстру. Часткова невдача лишає користувача
`blocked`, щоб операцію можна було повторити.

## 10. Поза обсягом

Редагування компонентів чи історичних прийомів, пошук і теги, ручне
сортування, пагінація, спільні страви, групові чати, кілька фото, web/Mini App,
окрема база даних, OAuth, персональні часові пояси та UI для зміни `day_start`.
