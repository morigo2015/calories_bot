# PRD: Telegram-бот обліку калорій

## Проблема і мета

Користувачу потрібен швидкий спосіб фіксувати з’їдену їжу без ручного пошуку
калорійності та ведення окремого щоденника. Бот приймає короткий текст або одне
фото страви в особистій Telegram-групі, оцінює відсутні дані, записує результат
і показує поточний денний підсумок.

Продукт розрахований на одного визначеного користувача та одну визначену групу
або супергрупу.

## Користувацький сценарій

1. Користувач пише опис з’їденої їжі або надсилає одне фото з необов’язковим
   підписом.
2. Бот показує жирним калорії прийому, компактний опис із вагою та `#`-значенням
   на 100 г, а також підсумок доби.
3. Для кількох продуктів відповідь лишається у тому самому форматі з трьох
   рядків без розбивки за складниками.
4. Результат автоматично зберігається в Google Sheets.
5. Кнопка `Видалити` прибирає запис із таблиці й перераховує відповідну добу.

Команди `/start` і `/help` показують короткі правила та приклади. Команда `/day`
показує, з яких прийомів їжі сформувався поточний підсумок облікового дня.
Після введення `/` Telegram має пропонувати команди `/day` і `/help` у меню
бота; команди реєструються через Bot API під час запуску застосунку.

Відповідь бота надсилається без цитування або вставки оригінального повідомлення,
щоб не дублювати текст у групі. Це правило поширюється на успішні відповіді,
помилки, довідку та відповідь `/day`.

Стандартна відповідь на прийом їжі має три рядки:

```text
50 кк
Абрикос 3 шт ≈105 г #48
=== 955 кк
```

Перший рядок відображається жирним у Telegram. `≈` ставиться лише біля
оціненої загальної ваги; `#48` означає агреговану калорійність на 100 г.
Для фото без caption замість текстового опису використовується назва,
розпізнана моделлю. Для складених страв окрема розбивка за складниками не
показується.

Відповідь `/day` має компактний формат, придатний для читання в Telegram:

```text
=== 780 кк
7 кк кава — ×3
320 кк вівсянка з бананом
```

Однакові `meal_name` після нормалізації пробілів і регістру об’єднуються, їхні
калорії додаються, а для повторів показується `×N`. Групи йдуть у порядку першої
появи в Google Sheets. Якщо записів немає, бот повідомляє про це замість
порожнього списку.

## Правила повідомлень

- `#120` і `120#` без пробілів означають 120 ккал/100 г; у внутрішньому
  нормалізованому тексті це зберігається як `120 ккал/100г`.
- Також приймаються `кк`, `ккал`, змішані кириличні/латинські літери та `kcal`.
- `г`, `гр`, відмінки `грам`, варіанти з подвійною `м`, а також `g`, `gr`,
  `gram`, `grams` означають грами.
- Ціле число наприкінці тексту або перед комою, `;` чи переносом рядка
  автоматично вважається грамами.
- Число всередині опису не є вагою: `2 яйця`, `піца 30 см`.
- Десяткові значення та `#` із пробілом не підтримуються.
- Якщо ваги немає, бот оцінює її та позначає вагу `≈`.

У видимих відповідях бота використовується скорочення `кк`; повна форма
`ккал` залишається лише у внутрішній нормалізації та таблиці.

Приклади:

```text
сир 50                   → сир 50 гр
сир 50г 120#             → сир 50 гр 120 ккал/100г
#120 сир 50 г            → 120 ккал/100г сир 50 гр
2 яйця, хліб 50          → 2 яйця, хліб 50 гр
батончик 45g 380kcal/100g → батончик 45 гр 380 ккал/100г
```

Нерозпізнаний текст не записується. Некоректний формат повертає коротку підказку
і також не створює запису.

## Фото

- Підтримується одне звичайне Telegram-фото, не альбом і не документ.
- Caption обробляється за тими самими правилами, що й текстове повідомлення.
- Фото без caption дозволене: модель розпізнає страву та оцінює порцію.
- Явна вага або калорійність із caption має пріоритет над оцінкою за фото.
- Бот використовує найбільший доступний Telegram-розмір і зберігає JPEG у
  локальному `PHOTO_STORAGE_DIR`.
- Без caption компактний опис починається з розпізнаної назви страви.

## Обліковий день і доступ

Початок нового дня задається локальним часом. Типово повідомлення до 01:00
належать попередньому дню, а з 01:00 — новому. Перехід літнього часу має
враховуватися автоматично.

Бот реагує тільки на визначеного користувача в дозволеній групі. Приватні
повідомлення, канали, інші групи, користувачі та edited-message updates
ігноруються.

## Видалення

Успішна відповідь має inline-кнопку `Видалити`, прив’язану до Telegram message
ID та облікової дати. Після підтвердженого фізичного видалення рядка бот
перераховує суму, намагається прибрати вихідне повідомлення й замінює свою
відповідь на `Видалено` та `=== N кк`. Вихідне повідомлення видаляється ботом
лише якщо йому дозволяють права групи та обмеження Telegram (зокрема часовий
ліміт); невдача цього кроку не повертає запис до таблиці.

Якщо Sheets не підтвердив операцію, Telegram-повідомлення не змінюються.
Повторний callback є ідемпотентним. Після видалення також прибирається локальне
фото, якщо воно знаходиться всередині `PHOTO_STORAGE_DIR`; небезпечні зовнішні
шляхи не видаляються.

Системне видалення через меню Telegram не відстежується, оскільки звичайний Bot
API не надсилає груповим ботам update про видалення користувацького повідомлення.
Тому штатний спосіб видалення запису — кнопка під відповіддю бота; редагування
повідомлень не підтримується, а edited-message updates ігноруються.

## Google Sheets

| Колонка | Зміст |
|---|---|
| `timestamp` | Числовий date-time Google Sheets у `APP_TIMEZONE` |
| `day` | Нативна дата облікової доби у форматі `yyyy-mm-dd` |
| `meal_name` | Назва прийому їжі |
| `total_weight_g` | Загальна вага |
| `meal_kcal` | Калорії прийому |
| `kcal_per_100g` | Калорійність прийому на 100 г |
| `telegram_message_id` | Захист від повторного запису |
| `normalized_request` | Текст або caption після нормалізації |
| `request` | Оригінальний текст або caption |
| `photo_path` | Абсолютний локальний шлях до фото; порожньо для тексту |
| `items_json` | Складники, ваги, калорійність та ознаки оцінки |
| `estimated` | Чи містить результат оцінені значення |
| `model`, `effort` | Параметри аналізу |
| `input_tokens`, `output_tokens` | Використані токени |
| `llm_cost_usd` | Розрахована вартість запиту, якщо задані тарифи |

Порядок колонок у таблиці відповідає порядку в цій таблиці. Один користувацький
опис або фото створює один рядок. Денний підсумок окремо не зберігається. Точна
попередня схема без `day` автоматично мігрується зі збереженням усіх клітинок;
порожні дати нової схеми дозаповнюються на наступному старті. Інші схеми
відхиляються.

## Критерії приймання

- Явні вага й калорійність завжди мають пріоритет над оцінкою.
- Фото з caption і без caption створює коректну відповідь та локальний файл.
- Для фото в Google Sheets записується абсолютний `photo_path`.
- `timestamp` є date-time, `day` — date, а денна сума враховує межу 01:00.
- Складене повідомлення створює одну відповідь і один рядок.
- Повторне Telegram-повідомлення не створює дубль.
- Помилки аналізу чи збереження не змінюють денний підсумок.
- Недозволені джерела не викликають аналіз і не створюють записів.
- Дані можна перевірити в Google Sheets, а відповідь зрозуміла без відкриття
  таблиці.
- `/day` використовує ту саму межу облікового дня, показує загальну суму та
  згрупований список без часу й іншої інформації.
- Відповіді бота не цитують і не вставляють оригінальне повідомлення користувача.
- Введення `/` відкриває в Telegram підказки `/day` і `/help`.
- Видалення прибирає рядок і локальне фото, оновлює підсумок та не змінює
  Sheets/Telegram при підтвердженій помилці запису.
- Успішні й помилкові відповіді не цитують оригінальне повідомлення.

## Не входить у продукт

Історія діалогу, редагування записів, `/undo`, відстеження системного видалення
повідомлень, багатокористувацький режим, підтримка каналів, фотоальбоми,
зображення-документи, публічна роздача фото, вебінтерфейс, база даних, webhook
та адмін-панель.
