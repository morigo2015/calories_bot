## VPS deployment

- VPS SSH alias: `hetzner`.
- VPS SSH user: `igor`.
- Project directory: `/home/igor/calories-bot`.
- Project files are owned by `igor:igor`.
- Do not use `sudo` to update project files.
- Use `sudo` only for systemd operations.
- Project directories must remain writable by their owner, `igor`.

